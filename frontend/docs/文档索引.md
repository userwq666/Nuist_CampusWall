# Nuist CampusWall 前端文档索引（V2.0）

最后更新：2026-05-21

## 1. 核心文档
1. `frontend/doc/页面与路由说明.md`：页面范围、路由设计、访问权限。
2. `frontend/doc/接口联调手册.md`：前端调用口径、入参、返回、易错字段。
3. `frontend/doc/开发计划.md`：按阶段推进任务与每日目标。
4. `frontend/doc/样式与组件规范.md`：页面样式、组件拆分、命名规范。
5. `frontend/doc/调试与排障.md`：联调问题与排查路径。
6. `frontend/doc/开发日志.md`：每轮迭代记录与阶段结论。

## 2. 当前实现状态（V2.0 — 联调闭环）
1. 已有路由：`/login`、`/register`、`/post`、`/post/:id`、`/profile`、`/admin`。
2. 路由守卫完整：`guestOnly / requiresAuth / requiresAdmin`。
3. 认证状态：`token + userInfo` 存储于 Pinia + localStorage，刷新不丢失。
4. 请求封装：Axios 统一拦截器，三段式返回适配，ECONNREFUSED 友好提示。
5. 全部页面已接入真实接口，功能闭环。

## 3. 阅读顺序建议
1. 先看 `页面与路由说明.md`（先明确做什么）。
2. 再看 `接口联调手册.md`（明确如何调接口）。
3. 编码时参考 `样式与组件规范.md`。
4. 遇到报错直接查 `调试与排障.md`。
