# 调试与排障手册（V1.0）

最后更新：2026-04-15

## 1. 常用启动命令
1. 前端：
```powershell
cd D:\javacode\javaweb\Nuist_CampusWall\frontend
npm run dev
```
2. 前端构建：
```powershell
npm run build
```
3. 后端（根目录）：
```powershell
mvn spring-boot:run
```

## 2. 常见问题与处理
## Q1 登录提示“请求失败，请稍后重试”
1. 打开浏览器 F12 -> Network，看请求是否发出。
2. 若状态为 `CORS` 或 `blocked`：检查 Vite 代理。
3. 若状态为 `401/403/500`：看后端日志中的错误详情。

## Q2 登录成功后访问其它接口还是 401
1. 检查 localStorage 是否有 `token`。
2. 检查请求头是否带 `Authorization: Bearer ...`。
3. 检查 token 是否过期（后端默认 7200 秒）。

## Q3 管理员登录后跳后台失败
1. 检查 `userInfo.role` 是否为 `ADMIN`。
2. 检查是否存在 `/admin` 路由。
3. 检查后台页面组件是否存在并正确导入。

## Q4 上传接口返回错误
1. 确认请求是 `multipart/form-data`。
2. 字段名必须是 `file`。
3. `fileType` 仅支持 `POST/COMMENT/AVATAR`。
4. 图片大小超过 5MB 会返回 413。

## Q5 帖子/评论提交失败但看不出原因
1. 看后端返回的 `message` 文本。
2. 看响应中的 `code` 与 HTTP 状态码。
3. 常见是参数名写错（`fileID` vs `fileId`）。

## 3. 自测清单（每次提交前）
1. `npm run build` 必须通过。
2. 登录、注册至少各测 1 次。
3. token 清空后访问受保护页会被拦截。
4. 管理员和普通用户各测一次跳转逻辑。

## 4. 问题记录模板（建议）
```text
日期：
模块：
现象：
复现步骤：
期望结果：
实际结果：
定位原因：
修复方式：
回归结果：
```

