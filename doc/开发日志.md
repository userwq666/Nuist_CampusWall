# Nuist CampusWall 开发日志

## 2026-03-13

### 1. 已完成
1. 项目基础工程已创建，Maven 可编译通过。
2. `domain` 已建立基础实体：
   - `User`
   - `Post`
   - `Comment`
3. 枚举已建立：
   - `Role`
   - `UserStatus`
   - `PostStatus`
   - `CommentStatus`
4. 数据库初始化脚本已接入：
   - `src/main/resources/sql/db_init.sql`
   - `src/main/resources/sql/db_seed.sql`
5. 自动执行 SQL 的配置已接入：
   - `spring.sql.init.mode=always`
   - `spring.sql.init.schema-locations`
   - `spring.sql.init.data-locations`
6. 已补充 `spring-boot-starter-jdbc`，解决了"配置了 SQL 初始化但不执行"的问题。
7. 数据库表已成功创建。
8. 启动检查类已建立：
   - `src/main/java/com/nuist_campuswall/config/initCheck.java`
9. 完成代码注释规范化：
   - 所有 Java 实体类注释
   - 所有枚举类注释
   - SQL 脚本注释
   - 外键约束添加注释说明
10. 数据库约束优化：
    - 评论表新增 CHECK 约束 `ck_comment_reply_pair`
    - 确保 replyToCommentId 和 replyToUserId 同时为 NULL 或同时不为 NULL
11. 文档更新：
    - 创建 README.md 项目简介
    - 需求说明文档
    - 新增评论模块需求说明
    - 完善数据模型章节

### 2. 当前可运行能力
1. Spring Boot 可启动。
2. 应用启动可自动初始化表结构与管理员初始数据。
3. 数据库中应存在三张核心表：
   - `user`
   - `post`
   - `comment`
4. 数据库表具备完整的外键约束和检查约束。

### 3. 当前问题
1. 管理员初始化密码当前为明文 `123456`，后续登录模块完成时需改为密文。

### 4. 下一阶段任务
1. 完成后端分层骨架：
   - `controller`
   - `service` / `service.impl`
   - `mapper`
   - `dto` / `vo`
2. 优先实现账号模块：
   - 注册
   - 登录（JWT）
   - 获取当前用户信息 `/account/me`
3. 再实现帖子主链路：
   - 发帖
   - 帖子列表分页
   - 帖子详情
4. 最后补评论与个人中心。

### 5. 今日结论
项目已从"纯骨架阶段"进入"可持续开发阶段"，下一步开始接口开发（先做用户注册/登录）。
