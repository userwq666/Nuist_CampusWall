# Nuist CampusWall 开发日志

## 2026-03-14

### 1. 今日完成
1. **枚举类重构与集中管理**：
   - 创建 `domain/enums/` 目录，集中管理所有枚举类型
   - 将 `CommentStatus`、`TargetType` 移动到 `enums` 包
   - 统一枚举类注释风格为中文单行注释
   - `TargetType` 集成 MyBatis-Plus 的 `@EnumValue` 注解，支持自动映射

2. **Controller 层建立**：
   - 创建 `controller/account/AccountController.java`
   - 实现用户注册接口 `/api/account/register`
   - 使用 `@RestController`、`@RequestMapping`、`@PostMapping` 等注解
   - 采用 `@RequestBody` 接收 JSON 数据并自动转换为 DTO

3. **Service 层骨架**：
   - 创建 `service/account/AccountService.java` 接口
   - 定义 `register(RegisterDTO dto)` 方法

4. **代码注释规范化**：
   - 所有 Controller 方法添加详细中文注释
   - 解释各 Spring 注解的作用和使用原因
   - `User.java` 实体类字段添加中文单行注释
   - `application.properties` 配置文件添加中文注释

5. **数据库配置优化**：
   - 修复 `db_init.sql` 中 CHECK 约束的括号闭合问题
   - 为所有外键约束添加中文注释说明
   - 统一 SQL 注释为中文

6. **文档更新**：
   - 更新 README.md，补充技术栈（MyBatis-Plus）
   - 完善项目结构说明（增加 controller、service、enums 层）
   - 需求说明文档补充点赞记录表结构
   - 新增 TargetType 枚举说明

### 2. 当前项目架构
```
controller 层  →  service 层  →  mapper 层（待实现）
    ↓              ↓              ↓
  DTO 接收       业务逻辑       数据库操作
```

### 3. 当前进度
1. ✅ 数据库表结构完整（user、post、comment、user_like）
2. ✅ 实体类、枚举类定义完整
3. ✅ Controller 层框架搭建（账户模块）
4. ✅ Service 层接口定义
5. ⏳ Mapper 层待实现（MyBatis-Plus）
6. ⏳ 具体业务逻辑待实现

### 4. 遇到的问题与解决
1. **问题**: Controller 中误用 `javax.xml.transform.Result`
   - **解决**: 改为直接返回 `String`，后续可使用统一响应包装类
   
2. **问题**: SQL CHECK 约束括号不闭合
   - **解决**: 补充缺失的右括号，确保语法正确

### 5. 下一阶段任务
1. **实现 Mapper 层**：
   - 创建 `mapper/UserMapper.java`
   - 继承 MyBatis-Plus 的 `BaseMapper<User>`
   
2. **完善 Service 层**：
   - 创建 `service/impl/AccountServiceImpl.java`
   - 实现用户注册的完整逻辑（密码加密、唯一性校验）
   
3. **登录功能**：
   - 实现登录接口（JWT Token 生成）
   - 实现获取当前用户信息接口
   
4. **帖子模块**：
   - PostController
   - PostService
   - PostMapper
   
5. **统一响应格式**：
   - 创建 `Result<T>` 统一返回类
   - 统一异常处理

### 6. 今日结论
项目已从"基础架构阶段"进入"功能开发阶段"，控制器和服务层框架已搭建完成，下一步重点实现具体的业务逻辑和数据访问层。
