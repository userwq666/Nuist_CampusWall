# Nuist CampusWall 开发日志

## 2026-03-25
### 本日完成
1. 评论模块主链路完成：
   - `POST /api/comment/create`
   - `GET /api/comment/page`
2. 评论 DTO 参数校验完成：
   - `CreateCommentDTO`
   - `PageCommentDTO`
3. 评论服务逻辑完成：
   - 登录态校验
   - 目标帖子存在性校验
   - `Comment -> CommentVO` 映射
4. HTTP 测试脚本完善：`src/test/http/comment.http`
5. 全量文档与代码状态对齐（本次重写）

### 设计决策
1. 评论采用“扁平楼层 + 回复字段”模型，不做无限树结构。
2. 回复一致性由数据库 CHECK 约束保证。
3. 统一通过 `Result<T>` 返回业务结果。

### 待办
1. 点赞接口落地（帖子/评论）
2. 管理端治理接口
3. 登录/注册参数注解完善（如 `@Email`）

---

## 2026-03-23
### 本日完成
1. 帖子模块第二阶段完成：
   - `GET /api/post/page`
   - `GET /api/post/{id}`
2. MP 分页插件接入：
   - `MybatisPlusConfig`
   - `mybatis-plus-jsqlparser` 依赖
3. 帖子 VO 输出规范化：`PostVO`
4. 新增 `POST_NOT_FOUND(409)`

---

## 2026-03-18
### 本日完成
1. JWT 闭环完成：
   - `JwtUtil` 签发解析
   - `JwtAuthInterceptor` 鉴权拦截
   - `UserContext` 上下文
   - `WebMvcConfig` 拦截器注册
2. `/api/account/me` 完成
3. 登录响应升级为 `LoginRespVO`（`token + userInfo`）
4. 错误码集中管理：`ErrorCode`

---

## 2026-03-16
### 本日完成
1. 登录接口主链路完成：
   - 用户查询
   - 用户状态校验
   - 密码校验
2. 登录错误码完善：`403/404/405`

---

## 2026-03-14
### 本日完成
1. 注册接口主链路完成：
   - 用户名查重
   - 邮箱查重
   - BCrypt 加密
   - 用户入库
2. MyBatis-Plus 接入与使用
3. 统一返回体、全局异常处理落地
4. 文档体系首次建立

## 全局风险与行动
1. 风险：当前测试主要为手工 HTTP 用例。
2. 行动：后续增加单元测试/集成测试，提高回归稳定性。
