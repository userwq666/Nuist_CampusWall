# Nuist CampusWall 开发日志

## 2026-03-16

### 1. 账户模块迭代完成（登录）
1. 登录接口主链路完成：
   - `POST /api/account/login`
   - 用户名查询
   - 账号状态校验（禁用拦截）
   - BCrypt 密码匹配
   - 返回 `LoginVO`（不暴露密码）
2. 账户服务接口升级：
   - `AccountService.login` 返回值从 `String` 调整为 `LoginVO`
3. 登录错误码补齐：
   - `403`：用户名不存在
   - `404`：用户已被禁用
   - `405`：密码错误

### 2. 文档更新完成
1. 统一错误码规范：
   - `401`：用户名已存在
   - `402`：邮箱已存在
   - `403`：用户名不存在
   - `404`：用户已被禁用
   - `405`：密码错误
   - `500`：服务器异常
2. 更新以下文档以匹配代码实现：
   - README.md
   - API文档.md
   - 错误码说明.md
   - 部署说明.md
   - 测试记录.md
3. 修复文档中的链接格式和排版细节

### 3. 文档 - 代码一致性确认
1. 注册与登录接口实现与文档描述一致
2. 错误码在代码中使用 401/402/403/404/405，文档已同步更新
3. 全局异常处理逻辑与文档描述一致
4. 数据库表结构与数据字典一致

## 2026-03-14

### 1. 本次迭代完成
1. 注册接口主链路完成：
   - `POST /api/account/register`
   - 用户名查重
   - 邮箱查重
   - BCrypt 密码加密
   - 新用户入库
2. MyBatis-Plus 接入完成：
   - Boot4 starter 已切换到 `mybatis-plus-spring-boot4-starter`
   - Mapper 与 Service 查重逻辑基于 MP Wrapper 实现
3. 统一返回与异常处理完成：
   - `Result<T>`
   - `BusinessException`
   - `GlobalExceptionHandler`
4. 枚举方案确定：
   - 保留枚举表达语义
   - 使用 `@EnumValue` 实现数据库数值映射
5. DTO 命名规范统一：
   - `username/password/nickname/educationEmail`
6. 文档规范化完成：
   - README、需求说明、开发日志、数据字典、API文档、部署说明、测试记录对齐当前实现
   - 新增架构说明、错误码说明、里程碑计划

### 2. 关键设计决策
1. 查重位置放在 Service，而非 Controller。
2. 格式校验（后续注解）放 DTO 层，业务校验放 Service 层。
3. 点赞采用“行为记录表 + 计数字段”双轨方案。
4. 评论采用“扁平楼层 + 回复字段”，不做无限树结构。

### 3. 当前状态评估
1. 注册功能可用，可联调。
2. 文档结构已覆盖需求、日志、数据字典、接口测试入口。
3. 下一阶段可以进入登录与鉴权实现。

### 4. 下一步待办
1. JWT 生成与鉴权拦截。
2. `/api/account/me` 当前用户接口。
3. 统一错误码常量类。
4. DTO 参数校验注解（`@NotBlank/@Email/@Size`）。
5. 帖子、评论、点赞接口逐步落地。
