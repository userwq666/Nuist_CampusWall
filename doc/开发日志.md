# Nuist CampusWall 开发日志

## 2026-03-27
1. 账户接口 `/api/account/me` 统一为 `/api/account/my`。
2. 帖子模块补齐：`my/page`、`update`、`delete`。
3. 评论模块补齐：`my/page`、`delete`。
4. 点赞模块维持 `do/undo`，并完成原子计数更新版本。
5. 帖子详情对不可见帖子统一返回 `409`。
6. `comment/my/page` 改为不要求 `postId`（新 DTO：`MyPageCommentDTO`）。

## 2026-03-26
1. 点赞模块落地：`/api/like/do`、`/api/like/undo`。
2. 错误码补齐到资源与权限相关场景。

## 2026-03-25 及更早
1. 注册、登录、JWT、帖子与评论基础链路完成。
