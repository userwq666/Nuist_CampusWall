# Nuist CampusWall 开发日志（V4.0）

最后更新：2026-05-21
维护人：项目组（后端主导 + 前端联调）

---

## 2026-03-14｜项目初始化与注册主链路
### 目标
1. 完成后端最小可运行骨架。
2. 打通第一个业务闭环：用户注册。

### 本次实现
1. 建立基础分层：`controller / service / mapper / domain / dto / common`。
2. 完成注册接口：`POST /api/account/register`。
3. 接入密码加密（BCrypt）。
4. 落地统一返回体 `Result<T>` 与业务异常 `BusinessException`。

### 验证
1. 注册成功可入库。
2. 用户名重复、邮箱重复可返回业务异常。

### 备注
1. 确立"先后端闭环、再前端联调"的开发策略。

---

## 2026-03-16｜登录接口与账户基础能力
### 目标
1. 实现登录校验。
2. 建立账户模块基本错误码语义。

### 本次实现
1. 完成登录接口：`POST /api/account/login`。
2. 完成用户名查询、密码比对、状态校验。
3. 登录返回结构升级为：`token + userInfo`。
4. 建立初版错误码常量（账户相关）。

### 验证
1. 正确密码登录成功。
2. 用户不存在、密码错误、账号禁用可区分返回。

---

## 2026-03-18｜JWT 鉴权闭环
### 目标
1. 完成无状态鉴权。
2. 提供"当前登录用户"能力。

### 本次实现
1. 完成 `JwtUtil`（签发/解析）。
2. 完成 `JwtAuthInterceptor`（拦截 `/api/**`，放行注册/登录）。
3. 完成 `UserContext(ThreadLocal)` 透传用户信息。
4. 打通 `GET /api/account/my`。

### 验证
1. 带 token 可访问受保护接口。
2. 不带 token / 非法 token 会被拦截。

---

## 2026-03-19 ~ 2026-03-26｜内容主链路（帖子/评论/点赞）
### 目标
1. 完成校园墙核心业务功能。
2. 保证可分页、可维护、可回归。

### 本次实现
1. 帖子：创建、公开分页、我的分页、详情、修改、软删除。
2. 评论：创建、公开分页、我的分页、删除。
3. 点赞：点赞、取消点赞、重复操作校验。
4. 评论模型采用"扁平楼层 + 回复字段"方案。

### 关键设计决策
1. 删除策略采用软删除（状态位），不做物理删除。
2. 点赞去重由数据库唯一约束保障。
3. 查询主链路优先使用 MyBatis-Plus 条件构造。

### 验证
1. 内容模块主接口全部可跑通。
2. 关键异常场景（越权、重复操作、目标不存在）可稳定复现。

---

## 2026-03-28｜管理员治理模块
### 目标
1. 落地管理员可用的治理能力。
2. 保持服务职责清晰，避免单类膨胀。

### 本次实现
1. 管理员鉴权接口：`GET /api/admin/ping`。
2. 用户治理：分页、启用、禁用。
3. 帖子治理：分页、详情、启用、禁用。
4. 评论治理：分页、详情、启用、禁用。
5. 管理服务分包：`auth / user / post / comment`。

### 验证
1. 管理员 token 可调用治理接口。
2. 普通用户调用返回无权限。

---

## 2026-03-31 ~ 2026-04-01｜文件模块与资源生命周期
### 目标
1. 完成图片上传与业务绑定能力。
2. 保证文件可追踪、可清理、可复用。

### 本次实现
1. 新增 `file_asset` 表与实体映射。
2. 完成上传接口：`POST /api/file/upload`。
3. 支持三类文件：`AVATAR / POST / COMMENT`。
4. 完成文件绑定迁移：`temp -> 业务目录`。
5. 完成解绑策略：旧图回写 `TEMP`。
6. 完成定时清理：超时 `TEMP` 转 `DELETED` 并删除物理文件。

### 验证
1. 头像、帖子图、评论图上传与绑定可跑通。
2. 重复绑定、类型不匹配、超限上传能正确报错。

---

## 2026-04-03｜协议收口（HTTP + 业务码）
### 目标
1. 统一响应协议，便于前端长期维护。
2. 降低异常出口耦合。

### 本次实现
1. `Result<T>` 统一为三段式：`{ code, message, data }`。
2. 保留 HTTP 状态码语义（401/403/404/409/413/415/422/500 等）。
3. 新增映射类：`ErrorCodeToHttpStatus`（业务码 -> HTTP 状态）。
4. `GlobalExceptionHandler` 仅负责异常捕获与统一出参。
5. 前端 `request.js` 同步适配三段式返回。

### 回归与构建
1. 后端编译通过：`mvn -DskipTests compile`。
2. 前端构建通过：`npm run build`。
3. HTTP 用例口径统一为"HTTP 状态 + 业务 code 双断言"。

---

## 2026-04-15｜前端联调闭环（V1.0）
### 目标
1. 根据联调文档完成前端所有页面与后端接口的对接。
2. 补齐缺失页面、修复逻辑 bug、统一错误提示。

### 本次修改
1. **路由**：新增 `/profile` 路由，登录态保护。
2. **NavBar**：搜索栏回车/清除、个人资料下拉菜单、移动端导航增加"个人资料"入口。
3. **登录/注册**：`alert` -> `ElMessage`，注册增加用户名/密码/邮箱格式校验。
4. **帖子列表**：
   - 新增"我的"标签页，未登录时提示跳转登录。
   - 搜索指示器（URL query 驱动 `?search=`）。
   - 发布帖子弹窗（标题+正文+图片上传）。
   - 修复 `pageSize.value` 误用（`const` 当作 `ref`）。
5. **帖子详情**：
   - 新增编辑/删除帖子（仅自己的帖子可见）。
   - 评论树加载（楼层 + 回复嵌套）。
   - 点赞/取消点赞失败提示。
   - 删除评论失败提示。
6. **管理后台**：
   - 首次进入调 `adminPingApi` 验证权限，失败跳回首页。
   - 列名修正：`id` -> `userId`/`postId`/`commentId`（对齐后端 DTO）。
   - 用户/帖子/评论启停操作增加成功/失败消息。
7. **API 层**：新增 `updateMyInfoApi`；动态 `import()` 全部改为静态 import，消除构建警告。
8. **ProfileEditView**：头像上传 + 昵称/邮箱/密码修改，保存后刷新 store 信息。

### 验证
1. `npm run build` 构建通过，仅剩 chunk size 警告（单 bundle 预期内）。
2. 各页面功能闭环可联调自测。

### 备注
1. 本次为前端联调交付，样式已按小红书风格实现。
2. 图片 URL 回显依赖后端静态资源映射配置。

---

## 2026-05-21｜全量文档同步 + 性能优化（V4.0）
### 目标
1. 全量文档同步至前后端联调完成状态。
2. 修复首次加载性能问题。

### 本次修改
1. **文档全量更新**（doc/ 11 份 + frontend/doc/ 7 份 + README）：
   - 状态全面升级为"前后端联调已全部完成"。
   - 补充前端架构说明、性能优化记录。
   - 修复过时描述与版本号。
2. **性能优化**：
   - **根因修复**：移除 `index.html` 中 `fonts.googleapis.com` 外链（国内被墙，CSS link 阻塞渲染最长 2 分钟）。
   - 路由懒加载（每页独立 chunk）。
   - 移除全局 ~300 个图标注册。
   - Axios 超时 10s -> 5s + ECONNREFUSED 友好提示。
3. **修复 index.html title 乱码**：`У԰ǽ` -> `校园墙`。
4. **后端补充**：`WebMvcConfig` 添加 `/static/upload/**` 映射，支持前端图片回显。

### 验证
1. `npm run build` 构建通过。
2. 首次加载从 ~2 分钟降至 ~3s（受 ElementPlus 首屏解析影响，浏览器可缓存）。

## 当前项目状态（截止 2026-05-21）
1. 后端主功能闭环已完成。
2. **前端联调已全部完成**：登录注册 -> 帖子流 -> 详情评论 -> 个人信息 -> 管理治理，链路完整。
3. **文档已全量同步**：doc/ + frontend/doc/ + README 共 19 份文档均已对齐 V4.0。
4. 下一阶段聚焦答辩准备：演示脚本、故障预案、演示账号、PPT。
