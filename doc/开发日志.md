# Nuist CampusWall 开发日志

## 2026-03-14
### 本日完成
1. 初始化项目骨架与基础分层结构。
2. 完成注册主链路（Controller/Service/Mapper/DTO）。
3. 引入统一返回体 `Result<T>`、业务异常 `BusinessException`、全局异常处理器。

## 2026-03-16
### 本日完成
1. 完成登录接口与密码校验（BCrypt）。
2. 完成登录响应结构升级（`token + userInfo`）。
3. 建立首版错误码常量体系。

## 2026-03-18
### 本日完成
1. 完成 JWT 闭环：签发、解析、拦截、上下文透传。
2. 打通 `GET /api/account/my`。
3. 明确受保护接口统一走拦截器鉴权策略。

## 2026-03-19
### 本日完成
1. 完成帖子发布接口与参数校验。
2. 完成帖子软删除基础策略（`status=DISABLE`）。

## 2026-03-23
### 本日完成
1. 完成帖子分页与详情。
2. 落地 MyBatis-Plus 分页插件。

## 2026-03-25
### 本日完成
1. 完成评论模块：创建、公开分页、我的分页、删除。
2. 完成评论回复字段规则与分页查询链路。

## 2026-03-26
### 本日完成
1. 点赞模块落地：`/api/like/do`、`/api/like/undo`。
2. 完成点赞去重、目标校验、计数更新。
3. 补齐点赞错误码与 HTTP 回归用例。

## 2026-03-28
### 本日完成（管理员治理能力）
1. 管理员鉴权与用户管理接口落地。
2. 管理员帖子治理接口落地。
3. 管理员评论治理接口落地。
4. 管理员服务按 `auth/user/post/comment` 分包完成。
5. `admin.http` 回归场景补齐。

## 2026-03-31
### 本日完成（文件模块阶段）
1. 新增 `file_asset` 资源表与对应领域模型。
2. 完成 `/api/file/upload`，支持图片校验与本地落盘。
3. 完成文件绑定迁移：`temp -> post/{id}|comment/{id}|avatar/{id}`。
4. 完成业务解绑策略：删除/换图时回写 `TEMP`。

## 2026-04-01
### 本日完成（交付收口）
1. 头像链路升级：`account/my/update` 支持 `fileID` 绑定头像。
2. 完成文件定时清理任务：按 `updateTime` 清理超时 `TEMP`。
3. 错误码补齐文件域：`414~419`。
4. 回归通过账户/文件/帖子/评论/点赞/管理员主链路。
5. 文档全量同步至 V3.1。

## 当前结论
1. 后端主功能已闭环并通过核心回归。
2. 项目进入前端联调与答辩演示优化阶段。
