# Nuist CampusWall 开发日志

## 2026-03-14
### 本日完成
1. 初始化项目骨架与基础分层结构。
2. 完成注册主链路（Controller/Service/Mapper/DTO）。
3. 引入统一返回体 `Result<T>`、业务异常 `BusinessException`、全局异常处理器。

## 2026-03-16
### 本日完成
1. 完成登录接口与密码校验（BCrypt）。
2. 完成登录响应结构升级（`token + userInfo`）。
3. 建立首版错误码常量体系。

## 2026-03-18
### 本日完成
1. 完成 JWT 闭环：签发、解析、拦截、上下文透传。
2. 打通 `GET /api/account/my`。
3. 明确受保护接口统一走拦截器鉴权策略。

## 2026-03-19 ~ 2026-04-01
### 阶段完成
1. 用户端帖子/评论/点赞主链路完成。
2. 管理员治理能力完成（用户/帖子/评论）。
3. 文件上传、绑定迁移、定时清理链路完成。

## 2026-04-03
### 本日完成（三段式回切）
1. `Result<T>` 恢复为 `{ code, message, data }`。
2. `GlobalExceptionHandler` 失败响应改为返回业务码（并保留 HTTP 状态码）。
3. 前端 `request.js` 调整为三段式处理。
4. 文档与 HTTP 用例回切到“HTTP + code 双断言”口径。

## 当前结论
1. 后端协议与测试口径已重新统一。
2. 可安心进入前端页面联调。