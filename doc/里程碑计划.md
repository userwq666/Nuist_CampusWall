# Nuist CampusWall 里程碑计划（2026 春）

## M1（已完成）账户与鉴权基座
1. 注册、登录、当前用户接口完成。
2. JWT 签发与拦截链路完成。
3. 统一返回与异常体系完成。

## M2（已完成）用户端主链路
1. 帖子：发布、分页、详情、修改、删除。
2. 评论：创建、分页、我的分页、删除。
3. 点赞：点赞、取消点赞、去重校验。

## M3（已完成）公告分流与管理员骨架
1. 公告接口 `GET /api/post/notice/page` 落地。
2. 管理员鉴权接口 `GET /api/admin/ping` 落地。
3. 管理员用户分页 `GET /api/admin/user/page` 落地。

## M4（已完成）管理员治理能力
1. 用户治理：分页 + 启用/禁用。
2. 帖子治理：分页 + 详情 + 启用/禁用。
3. 评论治理：分页 + 详情 + 启用/禁用。
4. 管理端 HTTP 回归用例补齐并通过。

## M5（已完成）文件模块与交付收口
1. 文件上传与业务绑定迁移完成。
2. 头像支持 `fileID` 绑定更新。
3. 文件生命周期与定时清理策略落地。
4. 全量文档同步到 V3.1。

## M6（下一阶段）前端联调与答辩材料
1. 完成登录、信息流、帖子详情、管理页基础界面。
2. 完成前后端联调与错误码提示映射。
3. 固化答辩演示路径、准备问答稿与风险预案。
