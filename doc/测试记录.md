# Nuist CampusWall 测试记录（V3.1）

## 1. 测试方式
1. 工具：IntelliJ IDEA HTTP Client + 局部 PowerShell/curl 验证。
2. 用例目录：`src/test/http/`。
3. 环境：本地 MySQL + 本地 Spring Boot 服务。

## 2. 用例覆盖统计
1. `account.http`：14 条
2. `file.http`：7 条
3. `post.http`：17 条
4. `comment.http`：13 条
5. `like.http`：8 条
6. `admin.http`：36 条
7. 总计：95 条

## 3. 关键覆盖点
### 3.1 用户端
1. 注册：成功、用户名重复、邮箱重复。
2. 登录：成功、密码错误。
3. 当前用户：带 token 成功、无 token 失败。
4. 个人信息修改：昵称/邮箱/密码/头像 `fileID` 更新。
5. 文件上传：`POST/COMMENT/AVATAR` 三类上传与边界校验。
6. 帖子：发帖、分页、公告分页、详情、修改、删除、重复绑定校验。
7. 评论：创建、分页、我的分页、删除、重复绑定校验。
8. 点赞：点赞、取消、重复操作、未登录、目标不存在。

### 3.2 管理端
1. 鉴权：管理员成功、普通用户 `412`、未登录 `406`。
2. 用户治理：分页、按状态筛选、启用/禁用、重复启用/禁用。
3. 帖子治理：分页、筛选、详情、启用/禁用、重复操作。
4. 评论治理：分页、筛选、详情、启用/禁用、重复操作。

## 4. 本轮重点验证（2026-04-01）
1. 头像文件链路：`upload(AVATAR) -> my/update(fileID) -> my(imageUrl)` 已通过。
2. 文件生命周期：`TEMP -> BOUND -> TEMP -> DELETED` 策略已在代码落地并可验证。
3. 路径编码问题已通过英文目录配置规避（`app.file.local-root=D:/campuswall_upload`）。

## 5. 结果结论
1. 后端主链路回归通过，无阻断性缺陷。
2. 当前版本可支撑课程演示、答辩与前端联调。
3. 后续重点从“后端实现”转向“联调与交付表达”。
