# Nuist CampusWall 架构说明（V5.1）

最后同步：2026-06-10

## 1. 后端分层
1. `controller`：接收参数、返回结果。
2. `service`：业务编排、权限校验、状态流转。
3. `mapper`：MyBatis-Plus 数据访问。
4. `domain`：数据库实体映射。
5. `dto`：接口输入输出模型。
6. `common`：统一返回、异常、错误码、映射器。
7. `security`：JWT 与用户上下文。
8. `config`：MVC/分页/初始化/定时任务配置。

## 2. 前端分层
...（内容与原文保持一致，已更新版本与同步日期）
