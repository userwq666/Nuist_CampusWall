### 本次修改

1. **文档全量更新（2026-06-10）**：doc/ 11 份 + frontend/doc/ 7 份 + README。已将文档口径与当前开发实现对齐，版本统一为 V5.1。
2. **NUIST 品牌与视觉调整（已生效）**：主题色、favicon、登录/注册样式、导航品牌等已同步到前端样式。
3. **性能优化记录**：移除 Google Fonts 外链、路由懒加载、Axios 超时优化、移除全局多余图标等。
4. **修复与回归验证**：图片上传修复、首次渲染性能回归、Playwright 可见浏览器用例通过验证。

---

### 验证
1. `npm run build` 构建通过。
2. Playwright 最近运行：32/32 全部通过（2026-05-26）。

## 当前项目状态（截止 2026-06-10）
1. 后端主功能闭环已完成。
2. **前端联调已全部完成**：登录注册 -> 帖子流 -> 详情评论 -> 个人信息 -> 管理治理，链路完整。
3. **NUIST 品牌主题已注入**：蓝色主调 + 校名标识 + 品牌 favicon。
4. **文档已全量同步**：doc/ + frontend/doc/ + README 共 19 份文档均已对齐 V5.1。
5. 下一阶段聚焦答辩准备：演示脚本、故障预案、演示账号、PPT。
